# Margin 0.1.15 · 写作体验与续写衔接

适用于 **Apple Silicon（M 系列）Mac，macOS 13+**。下载 `Margin-0.1.15-macOS-arm64.dmg`，不是 `Source code`。

## 本次更新

- 暖白稿纸、深色导航、紫色 AI 操作强调；轻量过渡与采用反馈，支持系统“减少动态效果”。
- 设置内调整正文字体、字号和行距，不增加主界面模块、不改变原稿与导出。
- 6 种选段改写模板及可编辑要求；候选展示文字差异，确认后才替换原稿。
- 改进人物状态、近期事件和相关历史的有预算检索，明确区分意图、已发生事件与角色认知。
- 拆书方法可参与场景规划与正文生成，核查使用原文片段依据；不把参考书情节当作本书事实。
- 修复确认联动修改后，下一章暂时无法参考刚修改正文的问题；旧派生记忆仍作废，后续未核对旧稿继续隔离。

## 测试边界

1076 项离线单测通过，相关模拟点击回归与桌面构建通过。最终 DMG 已测试从 0.1.14 升级保留正文、离线保存、重启、备份导入导出及模型连接反馈；全部使用隔离测试数据，真实 AI 调用为 0。最新完整“规划 → 修改 → 续写”流程尚未完成真实付费模型验收；测试通过不代表模型不会遗忘、误读或写出质量不佳的内容。在线生成及核查由用户自己的模型账户计费。

## 安装及升级

1. 在旧版导出完整作品备份，另存重要临时候选，按 **⌘Q 完全退出**。
2. 打开 DMG，把 Margin.app 拖入 Applications 并替换旧版。
3. 从“应用程序”打开，确认版本为 **0.1.15**。不要删除 `Application Support/Margin`。

此包仍未获得 Apple Developer ID 签名或公证。如果系统阻止打开，请核实来源后按 Apple 官方指引，在“系统设置 → 隐私与安全性”查看针对该应用的“仍要打开”。不要全局关闭系统安全保护。

没有自动更新、Windows 或 Intel Mac 安装包。原版 Release 保留。作品、小说原文和 API Key 不随此包上传 GitHub；在线调用会发送本次选择的材料。详见附件 `INSTALL-MAC.md`。
