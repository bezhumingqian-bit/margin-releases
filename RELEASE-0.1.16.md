# Margin 0.1.16 · 批量起草暂停恢复

适用于 **Apple Silicon（M 系列）Mac，macOS 13+**。下载 `Margin-0.1.16-macOS-arm64.dmg`，不是 Source code。

## 这次可以测试什么

进入“＋ 添加章节 → 按细纲批量起草”，打开原来暂停的批次：

- 检查未完成时可以“仅重试检查”，最多调用一次当前模型，可能计费；不会重新生成已保存正文。
- 有疑点时显示前文与候选依据，可编辑候选，或在核对后选择“已人工核对，允许继续”。人工确认不调用 AI，不代表模型检查通过，不自动写入长期记忆。
- 再点击“开始 / 继续起草”，只生成后面尚未完成的章节。退出应用再打开也能继续，已保存候选不会重新起草。
- 修改前面候选会使本章及后续候选的旧检查失效，但保留所有稿子，按顺序重新核对。

来源未变化的 0.1.15 批次可直接恢复；正文或设定已变化的批次仍需先下载候选再重新建批次，避免沿用过期材料。

## 升级

1. 在旧版导出完整作品备份，另存重要临时候选，按 **⌘Q 完全退出**。
2. 打开 DMG，把 Margin.app 拖入 Applications，替换旧应用。
3. 从“应用程序”打开，确认版本为 **0.1.16**。不要删除本地 Margin 数据目录。

仍未获得 Apple Developer ID 签名或公证。核实来源后，如果系统阻止打开，请按 Apple 官方指引在“系统设置 → 隐私与安全性”查看该应用的“仍要打开”。不要关闭全局安全保护。

## 验证范围

1087 项离线测试、3 组模拟浏览器回归通过；客户端验证批次落盘、重启恢复、仅重试检查、不重写已完成章节。全部使用隔离合成资料，真实 AI 调用为 0，不代表真实模型判断一定准确。作品、小说原文和 API Key 不随安装包上传；在线 AI 使用用户自己的账户计费。

无自动更新。旧版 Release 保留，不覆盖旧附件。本包不支持 Intel Mac 或 Windows。
