# Margin 0.1.16 · Mac 测试版

本包适用于 Apple Silicon Mac（M 系列芯片），macOS 13 或更新版本。不适用于 Intel Mac 或 Windows。

## 安装与首次打开

1. 先确认作品已保存并导出完整备份，用 **⌘Q 完全退出旧版**（不是只关闭窗口）。打开下载的 DMG，将 **Margin.app** 拖入 **Applications / 应用程序**。
2. 从“应用程序”中打开 Margin，不要长期直接从磁盘映像运行。
3. 本预览版没有 Apple Developer ID 签名及公证。首次运行如果提示无法验证开发者，在核实下载来源后，可按 Apple 官方指引，到“系统设置 → 隐私与安全性”查看针对 Margin 的“仍要打开”选项。
4. 如果系统明确提示恶意软件、应用损坏，或没有允许选项，请停止运行并反馈；不要全局关闭 Gatekeeper，也不要执行网上来源不明的绕过命令。

官方指引：https://support.apple.com/zh-cn/102445

## 开始写作

0.1.16 修复批量起草暂停后无法继续的问题。进入“＋ 添加章节 → 按细纲批量起草”，待核对的章节会展开：可以“仅重试检查”（最多一次模型请求，可能计费，不重写正文），或阅读前文和候选后选择“已人工核对，允许继续”（不调用 AI）。然后点击“开始 / 继续起草”，只起草尚未生成的章节。人工放行不是模型检查通过，不自动开始下一章，也不自动确认长期事实。

可在同一弹窗编辑候选；保存改动后，本章及后续已生成候选需重新核对，但原有候选不会被删除。退出并重新打开仍保留批次和人工确认。0.1.15 的未完成批次在来源未变化时可继续；如果正文或设定已变化，请先导出候选，再清除批次重新开始。升级前务必导出完整作品备份和重要候选。

本次使用合成资料完成 1087 项离线测试、3 组模拟浏览器回归及客户端恢复测试，没有调用真实付费模型。模型判断仍可能误报或漏报；下列 0.1.15 及更早说明按历史保留。

0.1.15 更新写作界面的配色、排版与轻量动效。在设置的偏好页面中调整正文字体、字号和行距；这些偏好只保存在本机，不改变原稿、导出内容或目标字数。系统开启“减少动态效果”时停用相关动画。

选段修改支持 6 种模板（改写、精简、动作表现、丰富描写、内心冲突、增强张力），也可直接输入要求。候选显示新增/替换文字及可展开的删除内容，确认采用前不改原稿。模板选择和差异展示本身不调用 AI。

改进人物状态、近期衔接和相关历史的有预算检索，以及拆书方法到场景规划、正文和证据核查的衔接。确认联动修改后，已检查并采用的本章新正文可供下一章参考，旧派生记忆仍作废；后续未核对章节继续隔离。如果本章之前已有待核对问题，一次局部修改不会自动清除。生成和额外核查仍可能计费，有限上下文与模型判断不能保证全书无误。

请先用测试作品验收。此次发布以离线、模拟点击与安装包测试为主；最新完整“规划 → 选段修改 → 续写”流程尚未用真实付费模型验收。升级前导出完整作品备份，另存重要临时候选，再完全退出旧应用。无自动更新。

以下保留历史版本说明；联动采用后前文可用性的规则以 0.1.15 为准。

0.1.14 去掉目录顶部的“章节管理”入口。直接右键左侧任意章节 → “删除章节” → 确认，即可移入已删除记录。右键菜单还提供“批量删除…”和“已删除章节…”；恢复旧正文不会自动恢复已清理的剧情记忆。支持章节获得焦点后按 Shift + F10 打开菜单、方向键选择、Escape 关闭；右键不会切换正在编辑的章节，删除对象是右键点中的那章。至少保留一章，正在写作或提取记忆时禁止删除。

0.1.13 移除正文下方独立的“修改选中段落”按钮。鼠标选中文字或使用 Shift + 方向键选择后，自动打开右侧选段修改区，填写要求后点击“帮我改这段”。重新选择同一段会保留尚未发送的修改意见；普通光标点击不展开。选中不调用 AI、不改正文；正在生成或已有未采用候选时，不用新选区覆盖当前任务。

0.1.12 将创作流程接入右侧助手：点击“规划新章节”（或目录“＋ 添加章节”），先讨论下一段的方向，规划 1–12 章；可继续提意见修订候选。确认方案后批量建章并准备顺序起草，再确认模型调用后开始。候选随作品保存，确认采用前不写入正文。原有“批量添加空白章”仍在操作下拉框中。

右侧“讨论剧情”会检索相关正文，并提出正文、大纲、人物和 Bible 的联动修改；先展示差异，作者确认后一起应用。“资料”中可撤回上次联动。选段联动也不再固定只看后两章，而是在预算内从全书检索相关章。一次最多读取 12 章、提出 12 处正文修改；会列出未读取数量，不保证已经读过或修复全书。更大范围请按章节继续讨论。

删除章节会从当前写作材料中清理该章及后续派生记录、本书纠正记忆和旧讨论；正文保留在回收记录以便恢复，但恢复不会恢复旧记忆。改写正文或更新设定也会保守清理相关派生记忆，并将受影响正文标记待核对；待核对正文不作为后续写作的前文证据，核对并重新提取确认后再使用。作者确认的跨书通用偏好、独立 Bible 设定不会因删除章节而凭空改写。旧 0.1.11 回收记录首次加载会自动完成记忆清理。历史版本及备份用于恢复，不是 AI 当前记忆。

以下是历史版本改动，记忆清理规则以 0.1.12 为准。

0.1.11 补上章节删除：进入“创作”，在左侧目录顶部点击“⋯ 章节管理”，默认选中当前章，也可多选或全选。删除前确认，移入“已删除”，可在同一弹窗选择恢复；尽量按原相邻章节位置恢复，标题不自动重编号。不直接永久删除正文，已删除内容及其历史随完整备份保留，但不用于正文导出或AI前文检索。至少保留一章；要替换全部内容，请先新增空白章。当前章节与已删除记录分别最多500条。

删除后停用该章来源的本书/本章记忆及关联资料，后续剧情记录和全书旧细纲标记待核对；作者明确保存的跨书通用偏好保留。恢复正文不会自动重新启用这些记忆。已有正文和Bible不自动改写。当前作品未采用的单章候选在确认后清除，请先下载；批次候选保留可下载，但不能继续或直接采用旧批次。操作不调用AI，请确认保存完成后退出；重要操作前建议导出完整备份。旧版软件不认识回收记录，请勿用旧版导入含已删除章节的新备份。

0.1.10 精简写作助手：常用界面只保留要求输入、AI主按钮与费用说明；章节方法、场景写法和参数统一收在“写作选项”，既有选择不变。AI按钮新增冷色渐变、悬停光泽与按压反馈；请求中显示光带及真实等待状态，不编造进度。候选以独立卡片淡入，采用前不改原稿；正文区不添加动画。系统开启“减少动态效果”时停用这些动画。该次改动不增加AI调用、不自动重写作品。

此次以当前0.1.8源码为基础制作独立0.1.10，未读取或合并来源尚未确认的0.1.9；旧DMG保留，不自动替换正在运行的软件。

0.1.8 打通拆书方法与创作。内置 19 条从《韩四当官》案例审读提炼的场景/章节方法，以及 3 条明确标注为综合提炼的阶段推进方法，不是 22 本书或原书完整章节结构。方法提供通用写法，不搬用原书人物、情节或结局。已归档的其他小说原文及完整私有分析不随安装包分发。

- 素材库 → 拆书方法：搜索、按层级筛选、启停、新建或编辑个人方法、本地 JSON 导入导出。编辑后的个人方法不再沿用原书出处；可恢复内置版本。库管理影响所有作品的后续请求。
- 创作 → 资料 → 方法库与推进参考，或故事筹备里的同名按钮：可关闭本书方法参考、选择来源，或指定优先参考的方法。无需额外填写这些选项也能创作。
- 阶段大纲使用结构层方法，整章与前台批量使用章节层方法，选段修改使用场景层方法。按本次要求匹配，否定要求优先；没有相关方法就不强行匹配。每次最多 2 条、约 1400 Token，并受整体资料预算限制。匹配在本机进行，不额外调用 AI。
- 候选的“实际发送的资料”、大纲候选和批量候选可查看方法快照。采用大纲后到概览查看最近方法记录；采用整章后在版本记录查看最近整章起草方法。快照只证明发送过，不证明模型一定使用，也不是当前正文的质量保证或本书事实。
- 导入方法包限 1 MB、最多 500 条；导入项默认停用，逐条复核后启用，不静默覆盖同 ID 的不同内容。导出前检查自建内容是否有私人信息；只生成本地文件，不上传公共库。方法库、作品设置和快照随完整作品备份保存。私有分析导入包与内置方法分开，草案不能冒充产品验证成果。

长篇记忆采用固定容量：单章和前台批次共用必要设定、作者规则、有效确认记录、近期衔接和相关旧章原文的装配路径。原文检索约 3400 Token、最多 10 处，另有覆盖范围说明；不会随整本小说的章节数无限增长。前文有修改、删除或顺序变化时重新取当前来源，正文标题不能冒充连续性检查的原文证据。未确认推断不写入事实，检索仍可能遗漏，不能保证第 14 章或更后章节绝不出错。

上下文规则升级后，旧版尚未完成的批次会暂停；先下载需要保留的候选，再新建批次，不会自动用新规则继续旧批次。

资料选择预算为 8000 Token，有前文证据时为 12000；每次模型请求另按系统提示、完整输入和至少 6000 输出预留检查 32000 Token 总估算预算。超过上限不发送该次请求，不静默删减核心事实。这是应用估算，不是服务商精确计数或账单，也不是一整次多步骤操作的费用上限。资料或方法设置在批次中改变会暂停旧批次，已有候选仍可下载。

0.1.7 增加前文连续性保护：单章生成、改稿和前台批量起草自动从本书之前的已写章节检索相关原文，不依赖手动勾选剧情记忆。优先包含上一已写章结尾以及与当前细纲相关的旧事件；改过的正文使用当前内容。批次之前尚未采用的候选会单独标明，不能当成已确认设定。

有前文时，生成后额外调用一次当前模型检查时间、已发生事件、身份和关系等，可能计费。发现冲突或检查未完成时，保留候选但阻止默认采用；可以把冲突填入修改意见重写，或人工核对后明确决定采用。批量遇到这些情况暂停，避免继续扩散。不会自动修改旧章或 Bible。检查只覆盖本次提供的有限原文，不是通读全书，也不能保证发现所有矛盾；“未发现冲突”不代表全书无误。已有错误正文不会因升级而自动重写。

0.1.6 简化创作界面：剧情进展不再铺在正文下方，在“创作 → 资料 → 查看 / 确认本章剧情记忆”按需打开。修改意见提取的长期规则也通过按钮打开确认弹窗。

选段候选点击“采用修改…”后，可仅替换选段，或额外调用一次 AI 检查并准备联动修改。联动范围为本章后文、随后最多两章已写正文，以及提供的设定与前 60 章细纲；查看差异并确认后才写入。原文变化、依据不匹配、重叠修改或待决问题会阻止应用。不保证识别所有影响，更远章节标记待核对。资料中可撤回当前会话最近一次联动（其后正文或资料变化时禁用）；各章修改前后正文快照随作品保存。重要改稿前仍建议导出完整备份。

目录“添加章节”统一提供：一次添加 1–50 个空白章（总数最多 500，零 AI 调用）；跳转现有后续 3–12 章细纲规划（仍有总计划 60 章限制）；按细纲顺序起草 1–12 个空白章。前台批量起草沿用本书目标，可暂停，失败停止，候选逐章保存；关闭应用停止后续请求，重新打开必须手动续跑。已发请求仍可能计费。候选确认后才加入对应空白章，不覆盖已有正文；资料变化后不能继续或直接采用旧批次，可先下载候选备份。

“资料 → 整理 / 分享通用写作规则”支持本地草稿、审核、规则 JSON 导入导出。不自动复制私人反馈与正文；请去除人名、隐私和特定设定。导入默认未审核、不启用；审核后可明确加入个人跨书偏好。导出只包含审核后的规则、适用场景、边界和自写示例，没有来源记录和作品标识，但需要作者自行确认文字已脱敏。没有公共上传服务。

强化当前章节标题、细纲与目标对人物行为的约束，旧稿不能压过作者明确的新要求；这属于提示词约束，不是模型训练或准确性保证。

0.1.5 新增“概览 → 本书写作设置”：一次设置本书每章默认目标字数 / 词数，现有及新增章节沿用，刷新和备份后保留。创作侧栏仍可临时调整本章，并可恢复本书默认；临时调整仅在当前工作区会话保留，刷新后恢复默认。不同作品互不影响，已有正文、已发送请求和候选稿的原目标不会被更改。目标仍是软参考，不保证模型精确达到。旧版网页后台批量起草沿用默认值，但仍受每章 100–3000 字 / 词的批量限制；桌面版不提供后台批量队列。

0.1.4 新增“修改意见 → 确认记忆 → 后续创作使用”：反馈和修改候选自动记录，采用与否独立记录；只有明确确认的记忆才用于生成。剧情纠正默认仅本章，通用偏好可选择本书或经再次确认跨书使用。到创作侧栏“资料 → 写作记忆与修改记录”编辑、停用、删除或查看来源。全工作区保留最近 40 次反馈记录，长文本最多保留前 20,000 字符；记忆最多 200 条。相同规则去重，相关规则提示人工核对，不保证自动识别所有矛盾。

移除无关的固定剧情示例和回车填入行为；回车仅换行。记忆随本机作品和备份保存，不上传公共库。生成请求会携带作用范围内的已确认记忆，并要求模型自检；这不是模型训练或独立质量验证，不保证永不重复犯错。规则提取沿用既有附加 AI 请求，可能计费，失败不会影响成功的改稿。

本次修复细纲生成仍沿用固定小额度的问题：按章节数分配 3,000–6,000 Token 输出上限，并要求紧凑、完整的章节 JSON。DeepSeek 明确使用非思考模式，避免思考占用答案额度；千问继续使用非思考模式。不能保证模型永不截断；失败不自动重试、不覆盖已有大纲和正文。

新增 DeepSeek（deepseek-flash / deepseek-v4-pro）和千问（qwen-plus / qwen-flash）模型选择，仍可手动填写完整 ID，更新不会自动更改已保存的模型。模型示例不代表账号权限，地区和模型支持以服务商为准。

保留模型连接的测试反馈：点击后显示“正在测试”，成功、失败和取消提示在按钮下方可见；不会自动更换密钥或回退到其他服务商。

保留 OpenAI 模型选择（GPT-4.1 / GPT-6 Astra），仍可手动填其他完整 ID。输入 `gpt-6` 会提示补全，不会自动替你切换模型或验证账号权限。

- 不需要注册账号；打开后是空白作品库。
- 新建作品即可离线编辑。需要 AI 时，进入“设置 → 模型连接”，填写你自己的服务商和 API Key。
- 已接入 MiniMax、OpenAI、DeepSeek、通义千问的预置官方站点；仅支持兼容当前文本接口的模型，不保证支持每种模型。
- 在线 AI 会将本次选定的正文和设定发送到你配置的服务商，并使用你的 API 额度。首次发送会提示确认。
- 网页端作品可先导出 JSON，再在“设置 → 备份与恢复”导入。导入会替换当前本机工作区，确认前请检查备份；替换前保留恢复点。

## 数据与备份

作品保存在这台电脑的 `~/Library/Application Support/Margin/library/`，不会自动同步云端或 GitHub。API Key 单独加密保存，不包含在作品备份中。作品文件本身未加密。

请经常在“设置 → 备份与恢复”导出作品备份，另存到其他磁盘。自动恢复点与作品在同一台电脑，不是异地备份。作品 JSON 包含确认记忆、最近修改记录（包括记录内尚未采用的候选）、前台批次候选和通用规则草稿；不包含通用素材库的个人定制、应用偏好、密钥或其他尚未采用的临时 AI 候选。重要候选请另行下载。不要将含私人正文或意见的备份公开分享。

测试版更新可能重新请求钥匙串权限。升级前保留自己的 API Key 与作品备份，不要把密钥发送给开发者。

## 从旧版升级

1. 在旧版导出作品备份，再退出 Margin。
2. 下载新版 DMG，将 Margin.app 拖入“应用程序”，确认替换旧应用。
3. 本机数据目录未变，作品和模型设置应继续保留；不要删除 `Application Support/Margin`。

当前没有自动更新。代码修改并不自动更新已安装的程序，需要下载安装新版本。旧版仍在 Release 页面保留；回退前也先备份作品。

## 当前限制

- 这是早期测试版，不是正式发行版；建议先用测试作品体验。
- 暂无 Windows / Intel Mac 包、自动更新、云同步、离线模型或后台批量生成队列。
- 本轮已验证本地创建、离线编辑、重启恢复、恢复点、模型设置；没有使用真实付费 Key 验收 AI 生成。
- 安装包不包含开发者的个人作品、原文小说库或 API Key。

反馈时请说明 Mac 芯片、macOS 版本、操作步骤与报错；不要附上密码、API Key 或不愿分享的正文。
